FROM ubuntu:22.04

RUN apt-get update && apt-get install -y curl && apt-get clean
COPY script.sh /usr/local/bin/script2.sh
RUN chmod +x /usr/local/bin/script2.sh

ENTRYPOINT ["/usr/local/bin/script2.sh"]